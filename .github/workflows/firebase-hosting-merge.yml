# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies and Deploy Functions # This is a descriptive name for the step
        run: |  # <--- THIS PIPE CHARACTER '|' AND THE INDENTATION ARE CRITICAL!
          cd functions # These lines are indented further
          npm ci       # Run clean install from there
          npm run deploy # Run the deploy script from your package.json
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SCAMSPARX }}
          channelId: live
          projectId: scamsparx